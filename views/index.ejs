<html>

<head>
  <link rel="stylesheet" href="/styles.css">
  <link rel="shortcut icon" href="favicon.png" type="image/x-icon">
  <title>Homepage</title>
</head>

<body>
  <header>
    <h1>Message Board</h1>
  </header>

  <main>
    <ul class="messages">
      <% messages.forEach((m)=> { %>
        <li>
          <span class="msg"><b><%= m.username %>: </b><%= m.text %></span>
          <span class="datetime"><%= m.added.toLocaleDateString() + ", " + m.added.toLocaleTimeString() %></span>
        </li>
            <% }); %>
    </ul>
    <div class="btn">
      <a href="/new">Write a new message</a>
    </div>
  </main>

  <footer>
  </footer>
</body>

</html>